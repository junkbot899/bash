#!/usr/bin/env bash

tir () {

	echo "tir.tbx[Texas Instruments Repo]"

	if [ -z "${1}" ]; then
		echo "\${1}: <comment> (git commit): Not provided!"
		exit
	fi


	if [ -z "${2}" ]; then
		echo "\${2}: <branch-name>: Not provided!"
		exit
	fi

	cd "/mnt/x/core/ti/projects/EK-TM4C1294XL"
	cp -r ../../custom_libraries ./custom_libraries/
	git add .
	git commit -am "${1}"
	git push origin "${2}"
}

tir "${1}" "${2}" 
